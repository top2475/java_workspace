day07
문자열 데이터
	- String 타입으로 변수 선언을 합니다.
	- String은 자바 클래스 .따라서 필드와 메소드를 갖습니다.
	- String으로 선언된 문자열은 객체로 만들어 집니다.
	- boolean,char,int,long,float,double 원시 타입과 다르게 
	String변수는 '참조타입'이라고 합니다.클래스 타입 변수는
	모두 참조 타입입니다.
	- String은 인스턴스 메소드와 클래스 메소드 모두 갖고 있습니다.
	- String으로 선언된 참조 타입 변수가 저장하는 값은 데이터를 바로 저장하는 
	원시 타입과 다른 특별한 값입니다.
	- 객체는 메모리 영역 중 heap 에 저장됩니다. String으로 선언된 참조 타입 
	변수는 이 객체를 찾아가기 위한 참조 값(like주소)을 저장합니다. 데이터를
	바로 변수에 저장하는 원시 타입과 다릅니다.

예시			message.length()				Math.max(4,5)

메소드 형식		static이 없는 인스턴스 메소드		static이 있는 클래스 메소드

메소드 실행		message 객체로 실행한다.			Math 클래스로 실행한다.

결론 			인스턴스 메소드는 객체를 만들어		클래스 메소드는 객체를 만들지 않고
			실행해야 한다.				클래스에 정의된 메소드를 실행한다.
			String message = "Hello";		형식은 클래스이름.메소드()
			객체를 다른 용어로 인스턴스라고		
			합니다.

ex) 아웃라인에서 보면 
    ()가 있는것은 String 메소드이고 ()가 
    없는것은 String 클래스에 있는 필드이다.