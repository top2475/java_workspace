char 와 관려된 연습으로 
아스키코드 정수값 이볅 0~127 범위로 받으세요.
입력 받은 값으로 아래 4개 중 하나로 

- 대문자 이다. 65~90
- 소문자 이다. 97~122
- 숫자 이다.  48~57
- 특수문자 이다. 나머지

switch 명령문 : 여러 개의 equal(==)값 조건식을 검사해야 하는 경우
		  if ~ else if 대신에 사용합니다.

{}중괄호 안쓰는 경우는 명령문에 ;세미콜론 기준으로 함
즉, ;세미콜론이 한개이면(조건식이 1개일때) {}중괄호를 쓰지 않는다.